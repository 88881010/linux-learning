    #info, man, coc
  
man -l arquivo ( para ler sem descompactar arquivo.)


/usr/share/doc/ e /usr/share/man/    ( contem arquivos de documentações oficiais)

formas de obter instrução, ( pelos comentarios nos arquivos de configuração) ( pelo /usr/share/man/ ou /usr/share/doc/ com o recurso < man -l para arquivos compactados. )


  #caminhos absolutos e caminhos relativos
   EX de caminho absoluto ( /usr/share/man )  EX diretorio relativo ( /documents )

    rmdir -rf diretorio (  remove o diretorio e tudo dentro dele )
rm -rf ( apaga arquivo e tudo dentro )

tar -tf arquivo.tar ( mostra dados do arquivo sem extrair)
ou
tar -tvjf arquivo ( mostra dados do arquivo bz2 )
tar -tvzf arquivo ( mostra dados do arquivo gz )      

tar -cvf arquivo ( compacta arquivo )

tar -cvzf arquivo ( comprimir arquivo para .gz)

tar -cvjf arquivo ( comprime arquivo para tar.bz2 )

tar -xvzf arquivo ( descomprime arquivo tar.gz )
tar -tvjf arquivo ( descomprime arquivo tar.bz2 )

gzip arquivo.tar  ( transforma arquivo tar para tar.gz )
gunzip arquivo.tar.gz ( torna o arquivo paara tar novamente )


bzip2 arquivo.tar ( transforma o arquivo tar para tar.bz2 )
bunzip2 arquivo.tar.bz2 ( torna o arquivo para .tar novamente )



  


apt install gzip bzip2

